
  # Personal QA Workspace App

  This is a code bundle for Personal QA Workspace App. The original project is available at https://www.figma.com/design/5QZPn024LUFnyubRIdIeUp/Personal-QA-Workspace-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  